<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        " http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.itheima.dao.MemberMapper">

    <resultMap id="BaseResultMap" type="com.itheima.pojo.Member">
        <!--@mbg.generated-->
        <id column="id" jdbcType="INTEGER" property="id" />
        <result column="fileNumber" jdbcType="VARCHAR" property="fileNumber" />
        <result column="name" jdbcType="VARCHAR" property="name" />
        <result column="sex" jdbcType="VARCHAR" property="sex" />
        <result column="idCard" jdbcType="VARCHAR" property="idCard" />
        <result column="phoneNumber" jdbcType="VARCHAR" property="phoneNumber" />
        <result column="regTime" jdbcType="DATE" property="regTime" />
        <result column="password" jdbcType="VARCHAR" property="password" />
        <result column="email" jdbcType="VARCHAR" property="email" />
        <result column="birthday" jdbcType="DATE" property="birthday" />
        <result column="remark" jdbcType="VARCHAR" property="remark" />
    </resultMap>
    <sql id="Base_Column_List">
        <!--@mbg.generated-->
        id, fileNumber, `name`, sex, idCard, phoneNumber, regTime, `password`, email, birthday,
        remark
    </sql>

    <!--auto generated by MybatisCodeHelper on 2020-01-14-->
    <select id="findOneByPhonenumber" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from t_member
        where phoneNumber=#{phonenumber,jdbcType=VARCHAR}
    </select>

    <insert id="add" parameterType="com.itheima.pojo.Member">

        <selectKey resultType="Integer" order="AFTER" keyProperty="id">
          SELECT LAST_INSERT_ID()
        </selectKey>

      insert into
      t_member
        (id,fileNumber,name,sex,idCard,phoneNumber,regTime,password,email,birthday,remark)
          values
            (null,#{fileNumber},#{name},#{sex},#{idCard},#{phoneNumber},#{regTime},#{password},#{email},#{birthday},#{remark})
    </insert>

<!--auto generated by MybatisCodeHelper on 2020-01-16-->
    <select id="findById" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from t_member
        where id=#{id,jdbcType=INTEGER}
    </select>

    <select id="getMemeberCountBeforeMonthMax" parameterType="string" resultType="integer">
        SELECT COUNT(id)
        FROM t_member
        WHERE regTime &lt; #{month}
    </select>

</mapper>